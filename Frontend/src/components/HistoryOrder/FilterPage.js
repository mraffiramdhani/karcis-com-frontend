import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class FilterPage extends Component {
  render() {
    return (
      <View>
        <Text> Ini FilterPage </Text>
      </View>
    );
  }
}
